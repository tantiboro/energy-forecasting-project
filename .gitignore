.terraform/
.terraform.lock.hcl
# --- Terraform ---
# Local .terraform directories (where the large providers live)
**/.terraform/*

# .tfstate files (contain sensitive infrastructure data - DO NOT COMMIT)
*.tfstate
*.tfstate.*

# Crash logs
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, 
# such as password, private keys, and other secrets.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# .terraform.lock.hcl usually SHOULD be committed, but if you are cross-platform
# having issues, some teams ignore it. For now, it's safe to keep it, so it's NOT listed here.


# --- Python ---
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Virtual Environments (very important to ignore!)
venv/
env/
ENV/
env.bak/
venv.bak/
.venv/

# Jupyter Notebooks
.ipynb_checkpoints

# --- OS / Editor / IDE ---
.DS_Store
.idea/
.vscode/
*.swp
*~

# --- Environment Variables ---
# NEVER commit these!
.env
.env.local
.env.*
venv/
*.pyc
__pycache__/
.env
terraform/.terraform/
terraform/*.tfstate*
terraform/.terraform.lock.hcl
*.pkl
*.json
model_training_function/model.json
delete_table.py
fix_table.py
